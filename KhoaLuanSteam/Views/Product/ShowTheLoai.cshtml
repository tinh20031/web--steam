@model IEnumerable<KhoaLuanSteam.Models.LOAISANPHAM>

@* <ul class="list-group list-group-flush text-left">
    @{
        foreach (var item in Model)
        {
            <li class="list-group-item" style="padding: 10px 30px;">
                <a href="@Url.Action("SPTheoCD", "Product", new { @maCD = @item.MaLoai })" style="text-transform:uppercase;text-decoration:none ;
   font-family: Roboto,HelveticaNeue,Helvetica Neue,sans-serif ; font-weight:400;line-height:1.7 ">
                    @item.TenLoai
                </a>
            </li>
        }

    }
</ul> *@


<ul>
    @{
        foreach (var item in Model)
        {
            <li><a href="@Url.Action("SPTheoCD", "Product", new { @maCD = @item.MaLoai })">@item.TenLoai</a></li>
        }
    }
    <li><a href="@Url.Action("AllProductDiscounts", "Product")">Sản phẩm giảm giá</a></li>
</ul>
